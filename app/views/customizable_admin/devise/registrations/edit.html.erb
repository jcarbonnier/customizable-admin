<div class="row">
  <div class="col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3">
    <div class="well clearfix">
      <h2><%= t('header.my_account') %></h2>
      <hr/>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name),
                          html: {method: :put, class: 'form-horizontal'},
                          wrapper: :horizontal_form_wl) do |f| %>
          <%= devise_error_messages! %>
          <%= f.input :civility, collection: ['M', 'Mme', 'Melle'] %>
          <%= f.input :firstname, placeholder: t('activerecord.attributes.user.firstname') %>
          <%= f.input :lastname, placeholder: t('activerecord.attributes.user.lastname') %>
          <%= f.input :pseudonym, placeholder: t('activerecord.attributes.user.pseudonym') %>
          <%= f.input :email, placeholder: t('activerecord.attributes.user.email') %>
          <%= f.input :password, autocomplete: "off", placeholder: t('activerecord.attributes.user.password') %>
          <%= f.input :password_confirmation, autocomplete: "off", placeholder: t('activerecord.attributes.user.password_confirmation') %>
          <%= f.input :current_password, autocomplete: "off", placeholder: t('activerecord.attributes.user.password_current') %>
          <%= f.submit t('buttons.update'), class: 'btn btn-block btn-primary' %>
      <% end %>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3">
    <div class="well clearfix">
      <h3><%= t('header.delete_my_account') %></h3>
      <hr/>
      <%= button_to t('buttons.delete'),
                    registration_path(resource_name),
                    data: {confirm: "Are you sure?"}, method: :delete, class: 'btn btn-block btn-danger' %>
    </div>
  </div>
</div>
